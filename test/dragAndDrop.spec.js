var todoList = require('../src/dragAndDrop');
describe('CustomEvents', function() {
    beforeEach(function() {
    });
    it('mixin', function() {
        var myObj = {};
        expect(1).toBe(1);
    });
});
